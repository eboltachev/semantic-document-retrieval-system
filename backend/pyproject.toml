[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "semantic-doc-backend"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.115.0",
  "uvicorn[standard]>=0.30.0",
  "httpx>=0.27.0",
  "opensearch-py>=2.6.0",
  "pydantic-settings>=2.4.0",
  "playwright>=1.48.0",
  "pymupdf>=1.24.0",
  "python-multipart>=0.0.9"
]

[tool.setuptools]
packages = ["app"]
